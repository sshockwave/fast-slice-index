[package]
name = "fast-slice-index"
version = "0.1.0"
edition = "2024"

[dependencies]
no-panic = "0.1"
logic-macros = { path = "logic-macros" }

[lints.rust]
unsafe_code = "deny"
unfulfilled_lint_expectations = "forbid"

[lints.clippy]
allow_attributes = "forbid"

[profile.dev]
debug = false
